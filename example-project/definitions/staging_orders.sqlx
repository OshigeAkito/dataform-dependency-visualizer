config {
  type: "table",
  schema: "staging",
  description: "Raw order data from source system"
}

SELECT
  order_id,
  customer_id,
  order_date,
  total_amount,
  status
FROM `source_system.orders`
