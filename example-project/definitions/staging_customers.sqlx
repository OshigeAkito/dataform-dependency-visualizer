config {
  type: "table",
  schema: "staging",
  description: "Raw customer data from source system"
}

SELECT
  customer_id,
  customer_name,
  email,
  created_at,
  updated_at
FROM `source_system.customers`
